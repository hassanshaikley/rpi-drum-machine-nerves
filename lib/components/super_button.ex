defmodule RpiDrumMachineNerves.Component.Superbutton do
  use Scenic.Scene, has_children: false

  def add_to_graph(map, any, list) do
  end

  def info(_data) do
  end

  def verify(_any) do
  end
end
